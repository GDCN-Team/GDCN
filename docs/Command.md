## 指令系统

---

### Argument 和 Option

> Argument 即为参数, 格式为: `--[key]=[value]`  
> 使用例: !rate --stars=8 `将关卡rate成8星(假设关卡没有被rate)`

> Option 即为选项, 格式为 `--[flag]`  
> 使用例: !rate -delete `删除Rating`

> 通用测试指令: !test `输出 worked!`
